server:
  port: 8082

spring:
  application:
    name: flight-service
  datasource:
    url: jdbc:postgresql://localhost:5432/skywings
    username: skywings
    password: skywings
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379