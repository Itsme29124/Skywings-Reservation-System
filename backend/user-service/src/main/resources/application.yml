server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/skywings
    username: skywings
    password: skywings
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  secret: skywings_super_secret_jwt_key_change_in_production
  expiration: 604800000